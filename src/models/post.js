export default class Post {
    constructor(title, description, category, subCategory, user, address, type) {
      this.title = title;
      this.description = description;
      this.category = category;
      this.subCategory = subCategory;
      this.user = user;
      this.address = address;
      this.type = type
    }
  }