export default class User {
    constructor(name, email, password, city, role) {
      this.name = name;
      this.email = email;
      this.password = password;
      this.role = role;
      this.city = city;
    }
  }